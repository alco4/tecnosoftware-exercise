'use strict';

describe('myApp.companies module', function() {

  beforeEach(module('myApp.companies'));

  describe('companies controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var companiesCtrl = $controller('CompaniesCtrl');
      expect(companiesCtrlCtrl).toBeDefined();
    }));

  });
});